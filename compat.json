{
  "_comment": "Version floor for the version-sensitive Codex surfaces this repo leans on (file profiles, model_instructions_file replacement, agent TOML). doctor.ps1 FAILS below min. There is deliberately no ceiling: Codex ships every few days, and doctor's Codex probes are the per-version verification, run on every invocation.",
  "codex": {
    "min": "0.149.0"
  }
}
